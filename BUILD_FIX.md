# 🔧 Исправление ошибок сборки

## Ошибка: Cannot find module '../src/generated/prisma/client'

Эта ошибка возникает, когда Prisma Client еще не сгенерирован или путь импорта неправильный.

### Решение:

1. **Убедитесь, что Prisma Client сгенерирован:**
   ```bash
   cd /var/www/salary-manager
   npx prisma generate
   ```

2. **Проверьте, что файл существует:**
   ```bash
   ls -la src/generated/prisma/client.ts
   ```

3. **Если файл не существует, проверьте конфигурацию Prisma:**
   ```bash
   cat prisma/schema.prisma | grep -A 3 "generator client"
   ```
   
   Должно быть:
   ```prisma
   generator client {
     provider = "prisma-client"
     output   = "../src/generated/prisma"
   }
   ```

4. **Проверьте переменную окружения DATABASE_URL:**
   ```bash
   cat .env | grep DATABASE_URL
   ```
   
   Убедитесь, что она правильно настроена и база данных доступна.

5. **Попробуйте собрать проект снова:**
   ```bash
   npm run build
   ```

### Альтернативное решение: Исключить скрипты из сборки Next.js

Скрипты в папке `scripts/` не должны компилироваться Next.js. Убедитесь, что они исключены из сборки.

Проверьте `tsconfig.json` - скрипты должны быть включены, но Next.js не должен пытаться их компилировать во время сборки.

Если проблема сохраняется, можно временно переименовать или переместить скрипты:

```bash
# Переместить скрипты вне src
mkdir -p scripts-ts
mv scripts/*.ts scripts-ts/
```

Или исключить из сборки Next.js, добавив в `next.config.ts`:

```typescript
const nextConfig: NextConfig = {
  typescript: {
    ignoreBuildErrors: false, // Оставьте false для проверки типов
  },
  // Исключить scripts из сборки
  pageExtensions: ['tsx', 'ts', 'jsx', 'js'],
};
```

Но лучше всего - просто сгенерировать Prisma Client перед сборкой.

## Проверка перед сборкой

Выполните эти команды перед `npm run build`:

```bash
# 1. Генерация Prisma Client
npx prisma generate

# 2. Проверка подключения к БД
npx prisma db pull

# 3. Применение миграций (если нужно)
npx prisma migrate deploy

# 4. Сборка проекта
npm run build
```

## Автоматизация в deploy.sh

Скрипт `deploy.sh` должен автоматически генерировать Prisma Client перед сборкой. Убедитесь, что в скрипте есть:

```bash
npx prisma generate
npx prisma migrate deploy
npm run build
```

