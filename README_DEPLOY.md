# 📘 Инструкция по развертыванию Salary Manager

## 🎯 Что было сделано

1. ✅ Создана полная инструкция по развертыванию на Ubuntu Server
2. ✅ Обновлены редиректы для домена `otchet.24cybersyndicate.ru`
3. ✅ Настроена конфигурация PostgreSQL (локальная БД на сервере)
4. ✅ Созданы конфигурационные файлы для Nginx и PM2
5. ✅ Создан скрипт автоматического развертывания

## 📁 Созданные файлы

- **DEPLOYMENT.md** - Подробная инструкция по развертыванию
- **QUICK_START_RU.md** - Краткая инструкция на русском языке
- **nginx.conf** - Конфигурация Nginx для домена
- **ecosystem.config.js** - Конфигурация PM2
- **deploy.sh** - Скрипт автоматического развертывания
- **env.sample** - Обновлен с правильными значениями для production

## 🚀 Быстрый старт

### Вариант 1: Автоматическое развертывание (рекомендуется)

1. Подключитесь к серверу:
   ```bash
   ssh root@93.183.82.104
   # Пароль: gkzF.t_TRSkR2N
   ```

2. Клонируйте репозиторий:
   ```bash
   mkdir -p /var/www
   cd /var/www
   git clone <ваш-репозиторий> salary-manager
   cd salary-manager
   ```

3. Запустите скрипт развертывания:
   ```bash
   chmod +x deploy.sh
   ./deploy.sh
   ```

4. Следуйте инструкциям скрипта (введите пароль для БД, настройте .env)

### Вариант 2: Ручное развертывание

Смотрите подробную инструкцию в файле [QUICK_START_RU.md](./QUICK_START_RU.md)

## 🔧 Основные настройки

### Переменные окружения (.env)

Файл `env.sample` обновлен с правильными значениями для production:

- ✅ `NEXTAUTH_URL="https://otchet.24cybersyndicate.ru"`
- ✅ `AUTH_URL="https://otchet.24cybersyndicate.ru"`
- ✅ `DATABASE_URL` настроен для локального PostgreSQL
- ✅ Все редиректы указывают на правильный домен

**Важно:** После копирования `env.sample` в `.env`:
1. Сгенерируйте случайные секреты: `openssl rand -base64 32`
2. Установите надежный пароль для пользователя БД
3. Установите `ALLOW_REGISTRATION="false"` после создания первого администратора

### PostgreSQL локально

**Да, PostgreSQL устанавливается локально на сервере** - не нужно покупать отдельную БД!

Скрипт автоматически:
- Устанавливает PostgreSQL
- Создает базу данных `salary`
- Создает пользователя `salary_user`
- Настраивает права доступа

### DNS настройки

Убедитесь, что в настройках DNS добавлена A-запись:
```
otchet.24cybersyndicate.ru → 93.183.82.104
```

### SSL сертификат

После развертывания получите SSL сертификат:
```bash
certbot --nginx -d otchet.24cybersyndicate.ru
```

Выберите вариант 2 (Redirect) для автоматического редиректа HTTP → HTTPS.

## 📋 Чек-лист развертывания

- [ ] Подключение к серверу выполнено
- [ ] Репозиторий клонирован в `/var/www/salary-manager`
- [ ] Запущен скрипт `deploy.sh` или выполнено ручное развертывание
- [ ] Настроен файл `.env` с правильными значениями
- [ ] Сгенерированы секреты (NEXTAUTH_SECRET, AUTH_SECRET, ADMIN_SETUP_SECRET)
- [ ] База данных создана и миграции применены
- [ ] Приложение собрано (`npm run build`)
- [ ] PM2 запущен и настроен автозапуск
- [ ] Nginx настроен и перезагружен
- [ ] DNS A-запись настроена
- [ ] SSL сертификат получен
- [ ] Приложение доступно по адресу `https://otchet.24cybersyndicate.ru`

## 🛠️ Управление приложением

### Просмотр логов
```bash
pm2 logs salary-manager
```

### Перезапуск приложения
```bash
pm2 restart salary-manager
```

### Обновление приложения
```bash
cd /var/www/salary-manager
git pull
npm install
npx prisma generate
npx prisma migrate deploy
npm run build
pm2 restart salary-manager
```

### Резервное копирование БД
```bash
# Настройка автоматического резервного копирования (см. DEPLOYMENT.md)
```

## 🔒 Безопасность

1. **Измените пароль root** после первоначальной настройки
2. **Настройте SSH ключи** вместо пароля
3. **Установите fail2ban** для защиты от брутфорса
4. **Регулярно обновляйте систему**: `apt update && apt upgrade -y`

## 🆘 Решение проблем

### Приложение не работает
```bash
# Проверить логи
pm2 logs salary-manager
tail -f /var/log/nginx/error.log

# Проверить статус сервисов
systemctl status nginx postgresql
pm2 status
```

### Проблемы с базой данных
```bash
systemctl status postgresql
su - postgres
psql -d salary
```

### Проблемы с портами
```bash
netstat -tulpn | grep :3000
netstat -tulpn | grep :80
netstat -tulpn | grep :443
```

## 📚 Дополнительная информация

- **Полная инструкция**: [DEPLOYMENT.md](./DEPLOYMENT.md)
- **Быстрый старт**: [QUICK_START_RU.md](./QUICK_START_RU.md)

## ✨ Особенности

- ✅ PostgreSQL установлен локально на сервере (не требует отдельной покупки)
- ✅ Все редиректы настроены на `https://otchet.24cybersyndicate.ru`
- ✅ Автоматическая настройка SSL через Let's Encrypt
- ✅ Автоматическое резервное копирование БД (опционально)
- ✅ PM2 для управления процессами и автозапуска
- ✅ Nginx для проксирования и SSL терминации

---

**Готово к развертыванию! 🚀**

Все редиректы настроены правильно, PostgreSQL будет работать локально на сервере, и вы сможете развернуть приложение без лишних проблем.

