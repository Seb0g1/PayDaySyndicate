# 🔧 Решение конфликтов Git на сервере

## Проблема

При выполнении `git pull` возникли конфликты:
- Локальные изменения в `nginx.conf`
- Конфликт с файлом `fix-postgres.sh`

## Решение

Выполните на сервере по порядку:

### Вариант 1: Сохранить локальные изменения и обновиться (рекомендуется)

```bash
cd /var/www/salary-manager

# 1. Сохранить текущие изменения nginx.conf
cp nginx.conf nginx.conf.backup

# 2. Удалить или переместить конфликтующий файл
rm fix-postgres.sh
# Или, если хотите сохранить: mv fix-postgres.sh fix-postgres.sh.local

# 3. Отменить локальные изменения в nginx.conf (или можно оставить, если нужны)
git checkout nginx.conf

# 4. Обновить код
git pull

# 5. Если нужно, восстановить ваши изменения в nginx.conf
# Сравните файлы:
diff nginx.conf nginx.conf.backup
# Или восстановите, если нужно:
# cp nginx.conf.backup nginx.conf

# 6. Продолжить обновление
npm install
npx prisma generate
npm run build
pm2 restart salary-manager
```

### Вариант 2: Принудительно получить версию из репозитория (если локальные изменения не важны)

```bash
cd /var/www/salary-manager

# 1. Удалить локальные изменения
git checkout nginx.conf
rm -f fix-postgres.sh

# 2. Обновить код
git pull

# 3. Продолжить обновление
npm install
npx prisma generate
npm run build
pm2 restart salary-manager
```

### Вариант 3: Использовать stash (временное сохранение изменений)

```bash
cd /var/www/salary-manager

# 1. Сохранить изменения во временное хранилище
git stash

# 2. Удалить конфликтующий файл
rm fix-postgres.sh

# 3. Обновить код
git pull

# 4. Если нужно, восстановить ваши изменения
git stash pop

# 5. Решить конфликты вручную, если они возникнут
# (обычно не нужно, если файлы не конфликтуют)

# 6. Продолжить обновление
npm install
npx prisma generate
npm run build
pm2 restart salary-manager
```

## Рекомендуемая последовательность (самый простой способ)

```bash
cd /var/www/salary-manager

# Сохранить текущую версию nginx.conf (на всякий случай)
cp nginx.conf nginx.conf.backup

# Отменить локальные изменения
git checkout nginx.conf

# Удалить конфликтующий файл
rm -f fix-postgres.sh

# Обновить код
git pull

# Установить зависимости
npm install

# Сгенерировать Prisma Client
npx prisma generate

# Применить миграции (если есть)
npx prisma migrate deploy

# Собрать проект
npm run build

# Перезапустить приложение
pm2 restart salary-manager

# Проверить логи
pm2 logs salary-manager
```

## Если нужно сохранить изменения в nginx.conf

Если вы вносили изменения в `nginx.conf` на сервере и хотите их сохранить:

```bash
# 1. Сравните вашу версию с версией из репозитория
cd /var/www/salary-manager
git diff nginx.conf

# 2. Если ваши изменения важны, объедините их вручную:
nano nginx.conf
# Отредактируйте файл, объединив ваши изменения с новыми из репозитория

# 3. Сохраните изменения в Git (опционально)
# git add nginx.conf
# git commit -m "Custom nginx configuration"
```

## Проверка после обновления

```bash
# Проверьте статус Git
git status

# Проверьте, что все файлы на месте
ls -la

# Проверьте работу приложения
pm2 status
pm2 logs salary-manager
```

