# 🔄 Обновление приложения на сервере

## Быстрое обновление (используйте скрипт)

```bash
cd /var/www/salary-manager
chmod +x update-server.sh
./update-server.sh
```

## Ручное обновление

Если скрипт не работает, выполните команды по порядку:

```bash
cd /var/www/salary-manager

# 1. Обновить код
git pull

# 2. Установить зависимости
npm install

# 3. КРИТИЧНО: Генерировать Prisma Client (ОБЯЗАТЕЛЬНО!)
npx prisma generate

# 4. Проверить генерацию
ls -la src/generated/prisma/client.ts
# Должен существовать файл client.ts

# 5. Применить миграции
npx prisma migrate deploy

# 6. Собрать проект
npm run build

# 7. Перезапустить приложение
pm2 restart salary-manager

# 8. Проверить логи
pm2 logs salary-manager
```

## Решение ошибки "Property 'langameSettings' does not exist"

Если вы видите эту ошибку, это означает, что Prisma Client не был сгенерирован после обновления кода.

**Решение:**

```bash
cd /var/www/salary-manager

# 1. Удалить старый сгенерированный Prisma Client
rm -rf src/generated/prisma

# 2. Сгенерировать заново
npx prisma generate

# 3. Проверить, что модель создана
ls -la src/generated/prisma/models/LangameSettings.ts
# Файл должен существовать

# 4. Пересобрать проект
npm run build

# 5. Перезапустить
pm2 restart salary-manager
```

## Проверка после обновления

```bash
# Проверить статус
pm2 status

# Проверить логи
pm2 logs salary-manager --lines 50

# Проверить работу приложения
curl http://localhost:3000/api/me
```

## Частые проблемы

### Проблема 1: Prisma Client не генерируется

```bash
# Убедитесь, что схема правильная
cat prisma/schema.prisma | grep -A 10 "model LangameSettings"

# Попробуйте удалить node_modules и переустановить
rm -rf node_modules
npm install
npx prisma generate
```

### Проблема 2: Ошибка при сборке

```bash
# Очистить кэш Next.js
rm -rf .next

# Пересобрать
npx prisma generate
npm run build
```

### Проблема 3: Миграции не применяются

```bash
# Проверить подключение к БД
npx prisma db pull

# Применить миграции вручную
npx prisma migrate deploy --skip-generate
```

