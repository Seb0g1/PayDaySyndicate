# 🔧 Решение проблем при развертывании

## Ошибка: P1000: Authentication failed against database server

Эта ошибка означает, что учетные данные для подключения к PostgreSQL неверны.

### Решение 1: Создание/обновление пользователя PostgreSQL

1. **Переключитесь на пользователя postgres:**
   ```bash
   su - postgres
   ```

2. **Войдите в psql:**
   ```bash
   psql
   ```

3. **Проверьте существование пользователя:**
   ```sql
   SELECT usename FROM pg_user WHERE usename = 'salary_user';
   ```

4. **Если пользователь не существует, создайте его:**
   ```sql
   CREATE USER salary_user WITH PASSWORD 'ваш_надежный_пароль';
   ```

5. **Если пользователь существует, но пароль неверный, измените пароль:**
   ```sql
   ALTER USER salary_user WITH PASSWORD 'ваш_надежный_пароль';
   ```

6. **Проверьте существование базы данных:**
   ```sql
   SELECT datname FROM pg_database WHERE datname = 'salary';
   ```

7. **Если база данных не существует, создайте ее:**
   ```sql
   CREATE DATABASE salary;
   ```

8. **Назначьте владельца базы данных:**
   ```sql
   ALTER DATABASE salary OWNER TO salary_user;
   ```

9. **Выдайте все привилегии:**
   ```sql
   GRANT ALL PRIVILEGES ON DATABASE salary TO salary_user;
   ```

10. **Выйдите из psql:**
    ```sql
    \q
    ```

11. **Вернитесь к root:**
    ```bash
    exit
    ```

12. **Обновите файл .env с правильным паролем:**
    ```bash
    nano /var/www/salary-manager/.env
    ```
    
    Убедитесь, что DATABASE_URL содержит правильный пароль:
    ```env
    DATABASE_URL="postgresql://salary_user:ваш_пароль@localhost:5432/salary?schema=public"
    ```

### Решение 2: Использование скрипта автоматического исправления

Используйте скрипт `fix-postgres.sh`:

```bash
cd /var/www/salary-manager
chmod +x fix-postgres.sh
./fix-postgres.sh
```

### Решение 3: Проверка настроек pg_hba.conf

Если проблема сохраняется, проверьте файл `pg_hba.conf`:

1. **Найдите файл pg_hba.conf:**
   ```bash
   find /etc/postgresql -name pg_hba.conf
   ```

2. **Отредактируйте файл:**
   ```bash
   nano /etc/postgresql/*/main/pg_hba.conf
   ```

3. **Убедитесь, что есть следующие строки:**
   ```
   # TYPE  DATABASE        USER            ADDRESS                 METHOD
   local   all             all                                     md5
   host    all             all             127.0.0.1/32            md5
   host    all             all             ::1/128                 md5
   ```

4. **Перезапустите PostgreSQL:**
   ```bash
   systemctl restart postgresql
   ```

### Решение 4: Проверка подключения вручную

Проверьте подключение к базе данных:

```bash
# Установите переменную окружения с паролем
export PGPASSWORD='ваш_пароль'

# Попробуйте подключиться
psql -h localhost -U salary_user -d salary -c "SELECT 1;"
```

Если подключение успешно, проблема в файле `.env`. Проверьте:
- Правильность пароля в DATABASE_URL
- Отсутствие лишних пробелов
- Правильное экранирование специальных символов в пароле

### Решение 5: Полное пересоздание пользователя и базы данных

Если ничего не помогает, пересоздайте все с нуля:

```bash
# Переключитесь на пользователя postgres
su - postgres
psql

# Удалите базу данных и пользователя (если существуют)
DROP DATABASE IF EXISTS salary;
DROP USER IF EXISTS salary_user;

# Создайте заново
CREATE USER salary_user WITH PASSWORD 'новый_надежный_пароль';
CREATE DATABASE salary OWNER salary_user;
GRANT ALL PRIVILEGES ON DATABASE salary TO salary_user;

# Выйдите
\q
exit

# Обновите .env файл
nano /var/www/salary-manager/.env

# Примените миграции
cd /var/www/salary-manager
npx prisma migrate deploy
```

## Проверка после исправления

После исправления проблемы выполните:

```bash
cd /var/www/salary-manager

# Проверьте подключение
npx prisma db pull

# Примените миграции
npx prisma migrate deploy

# Генерируйте Prisma Client
npx prisma generate
```

## Другие распространенные ошибки

### Ошибка: Connection refused

**Причина:** PostgreSQL не запущен

**Решение:**
```bash
systemctl status postgresql
systemctl start postgresql
systemctl enable postgresql
```

### Ошибка: Database "salary" does not exist

**Причина:** База данных не создана

**Решение:** См. Решение 1 выше

### Ошибка: Permission denied

**Причина:** Пользователь не имеет прав на базу данных

**Решение:**
```sql
GRANT ALL PRIVILEGES ON DATABASE salary TO salary_user;
GRANT ALL ON SCHEMA public TO salary_user;
```

### Ошибка: Password authentication failed

**Причина:** Неверный пароль

**Решение:** Измените пароль пользователя (см. Решение 1)

## Полезные команды для диагностики

```bash
# Статус PostgreSQL
systemctl status postgresql

# Логи PostgreSQL
tail -f /var/log/postgresql/postgresql-*-main.log

# Список баз данных
sudo -u postgres psql -l

# Список пользователей
sudo -u postgres psql -c "\du"

# Подключение к базе данных от имени postgres
sudo -u postgres psql -d salary

# Проверка подключения с паролем
PGPASSWORD='пароль' psql -h localhost -U salary_user -d salary -c "SELECT version();"
```

## Получение помощи

Если проблема не решена:
1. Проверьте логи: `tail -f /var/log/postgresql/postgresql-*-main.log`
2. Проверьте статус сервисов: `systemctl status postgresql`
3. Убедитесь, что порт 5432 открыт: `netstat -tulpn | grep 5432`

