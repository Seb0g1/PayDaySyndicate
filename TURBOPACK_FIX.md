# 🔧 Исправление ошибки Turbopack при сборке

## Проблема

При сборке Next.js 16 возникает ошибка:
```
ERROR: This build is using Turbopack, with a `webpack` config and no `turbopack` config.
```

## Причина

Next.js 16 по умолчанию использует Turbopack для сборки, но конфигурации для него не было.

## Решение

### Вариант 1: Использовать webpack (рекомендуется для стабильности)

Обновлен `package.json` - скрипт сборки теперь использует флаг `--webpack`:

```json
"build": "next build --webpack"
```

Это заставит Next.js использовать webpack вместо Turbopack, что более стабильно.

### Вариант 2: Использовать Turbopack

В `next.config.ts` добавлена пустая конфигурация Turbopack:

```typescript
turbopack: {},
```

Это отключает предупреждение, но использует Turbopack.

## Что нужно сделать на сервере

```bash
cd /var/www/salary-manager

# 1. Обновите код
git pull

# 2. Установите зависимости (если нужно)
npm install

# 3. Пересоберите проект
npm run build

# 4. Перезапустите приложение
pm2 restart salary-manager
```

## Дополнительные замечания

### Предупреждение о middleware

Предупреждение `The "middleware" file convention is deprecated` не критично. Файл `src/middleware.ts` все еще работает, просто Next.js рекомендует использовать новый подход. Это можно игнорировать для текущей версии.

### Какой вариант выбрать?

- **Webpack** (`--webpack` флаг) - более стабильный, проверенный временем
- **Turbopack** - быстрее, но еще в разработке

Для production рекомендуется использовать webpack для максимальной стабильности.

