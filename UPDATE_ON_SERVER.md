# 🔄 Обновление кода на сервере

## Если репозиторий уже склонирован (обычный случай)

Если вы уже клонировали репозиторий ранее, просто обновите код:

```bash
# 1. Перейдите в директорию проекта
cd /var/www/salary-manager

# 2. Получите последние изменения из GitHub
git pull

# 3. Установите новые зависимости (если есть)
npm install

# 4. Сгенерируйте Prisma Client (если нужно)
npx prisma generate

# 5. Примените миграции (если есть новые)
npx prisma migrate deploy

# 6. Пересоберите проект
npm run build

# 7. Перезапустите приложение
pm2 restart salary-manager

# 8. Проверьте логи
pm2 logs salary-manager
```

## Если репозиторий еще не склонирован (первый раз)

Если это первая установка или вы удалили директорию, клонируйте репозиторий:

```bash
# 1. Перейдите в директорию www
cd /var/www

# 2. Клонируйте репозиторий
git clone https://github.com/Seb0g1/PayDaySyndicate.git salary-manager

# 3. Перейдите в директорию проекта
cd salary-manager

# 4. Установите зависимости
npm install

# 5. Создайте файл .env (скопируйте из env.sample)
cp env.sample .env
nano .env  # Отредактируйте с правильными значениями

# 6. Сгенерируйте Prisma Client
npx prisma generate

# 7. Примените миграции
npx prisma migrate deploy

# 8. Соберите проект
npm run build

# 9. Запустите с PM2
pm2 start ecosystem.config.js
pm2 save

# 10. Настройте Nginx
cp nginx.conf /etc/nginx/sites-available/salary-manager
ln -sf /etc/nginx/sites-available/salary-manager /etc/nginx/sites-enabled/
nginx -t
systemctl reload nginx
```

## Как проверить, склонирован ли уже репозиторий?

```bash
# Проверьте, существует ли директория
ls -la /var/www/salary-manager

# Проверьте, есть ли там .git
ls -la /var/www/salary-manager/.git

# Если директория существует и есть .git - значит репозиторий уже склонирован
# Просто выполните git pull
```

## Важные замечания

1. **Не удаляйте директорию** `/var/www/salary-manager` если там уже есть:
   - Файл `.env` с вашими настройками
   - База данных (если используется локальная)
   - Загруженные файлы в `public/uploads`

2. **Git pull безопасен** - он только обновит код, не затронет ваши файлы `.env` и загрузки (они в `.gitignore`)

3. **После git pull** всегда пересобирайте проект (`npm run build`) и перезапускайте PM2

## Быстрая команда для обновления

```bash
cd /var/www/salary-manager && git pull && npm install && npx prisma generate && npx prisma migrate deploy && npm run build && pm2 restart salary-manager
```

Это выполнит все шаги обновления одной командой.

