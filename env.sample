# PostgreSQL connection string (порт по умолчанию 5432 — не оставляйте слово PORT)
# Для локальной разработки:
# DATABASE_URL="postgresql://postgres:CGJ-Ge-90@localhost:5432/salary?schema=public"
# Для production на сервере (замените your_secure_password_here на реальный пароль):
DATABASE_URL="postgresql://salary_user:your_secure_password_here@localhost:5432/salary?schema=public"

# NextAuth/Auth.js
# Для локальной разработки:
# NEXTAUTH_URL="http://localhost:3000"
# Для production:
NEXTAUTH_URL="https://otchet.24cybersyndicate.ru"
# Сгенерируйте случайный секрет: openssl rand -base64 32
NEXTAUTH_SECRET="dlfgldfgldflgkd;fg"

# Auth.js v5 env names (supported by NextAuth v5)
# Для локальной разработки:
# AUTH_URL="http://localhost:3000"
# Для production:
AUTH_URL="https://otchet.24cybersyndicate.ru"
# Сгенерируйте случайный секрет: openssl rand -base64 32
AUTH_SECRET="dfglkdgl;djgdgkjdfg"
AUTH_GOOGLE_ID="your-google-oauth-client-id"
AUTH_GOOGLE_SECRET="your-google-oauth-client-secret"

# App config
# Для первоначальной настройки установите в true, затем измените на false
ALLOW_REGISTRATION="false"
# Сгенерируйте случайный секрет: openssl rand -base64 32
ADMIN_SETUP_SECRET="set-a-one-time-secret"
